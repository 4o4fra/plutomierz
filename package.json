{
  "name": "plutomierz",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "android"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=api\" \"npm run dev --workspace=web\""
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "concurrently": "^9.1.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
  }
}
